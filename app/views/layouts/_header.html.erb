<nav class="navbar navbar-expand-lg bg-primary">
    <div class= "container-fluid mx-auto"> 
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex">
                    <li class="nav-item"> <%= link_to 'Home', posts_path, class: "text-light nav-link"%> </li>

                    <li class="nav-item">
                    <%= link_to 'Find Friends', users_path, class: "text-light nav-link"%></li>
                    <% if user_signed_in? %>
                    <li class="nav-item">
                         <a class="nav-link text-white"  href="#"><%= current_user.notifications.count%>notifications</a>
                    </li>
                    
                </ul>

                <div class="float-right">
                 <%= link_to "Profile", user_path(current_user), class: "text-white mr-3"%> 
                    <%= link_to "Logout", destroy_user_session_path, class: "text-white", method: :delete %></li>
                    <% else %>
                    <%= link_to 'Login or SignUp', new_user_session_path, class:'text-white'%>
                </div>
                <% end %>
    </div>
           
</nav>
        
        <% if user_signed_in? %>
        
        <%end%>
     
